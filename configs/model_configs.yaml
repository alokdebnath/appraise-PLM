model_name: "roberta-base"  # Can be changed to any HuggingFace model
num_dimensions: 21
hidden_size: 768
dropout: 0.1
learning_rate: 3e-5
batch_size: 32
num_epochs: 10
warmup_steps: 100
max_length: 512 